package ui

templ Divider() {
	<div class="pt-3 border-t border-border-divider"></div>
}

templ SectionHeader(title string) {
	<h3 class="text-[11px] font-semibold text-content-muted uppercase mb-2 tracking-wide">{ title }</h3>
}
